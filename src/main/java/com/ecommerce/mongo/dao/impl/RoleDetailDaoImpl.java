package com.ecommerce.mongo.dao.impl;

import org.springframework.data.mongodb.core.MongoOperations;
import org.springframework.stereotype.Repository;

import com.ecommerce.model.constants.CollectionName;
import com.ecommerce.model.roledetail.RoleDetail;

public class RoleDetailDaoImpl {
	/*
	 * 
	 * private final MongoOperations mongoOperations;
	 * 
	 * public RoleDetailDaoImpl(MongoOperations mongoOperations) {
	 * this.mongoOperations = mongoOperations; }
	 * 
	 * @Override public RoleDetail SaveRoleDetail(RoleDetail roleDetail) { return
	 * mongoOperations.save(roleDetail, CollectionName.ROLE_DETAIL); }
	 * 
	 * @Override public RoleDetail findById(String id) { return
	 * mongoOperations.findById(id, RoleDetail.class); }
	 * 
	 */}
